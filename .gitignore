/target
**/*.rs.bk
conformance/media/
conformance/golden/
native/out/

# Release bundles: lumen.exe is small but mpv.exe is 115 MB. Built by
# crates/lumen-play/package-windows.sh, not committed.
/dist/

# Android build output and local SDK config
android/.gradle/
android/build/
android/app/build/
android/local.properties
# The debug keystore is generated by CI or locally; a signing key of any kind stays out of git.
android/app/debug.keystore
