<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A no-action-bar theme: Compose draws all the chrome, and a platform action bar on top of it
      would steal vertical space that the cover screen cannot spare.
    -->
    <style name="Theme.Lumen" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
